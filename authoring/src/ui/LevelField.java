package ui;

/**
 * Enum class to define the property fields available to AuthoringLevel objects in the ByteMe Authoring Environment UI
 * @author Harry Ross
 */
public enum LevelField {
    LABEL, WIDTH, HEIGHT, BACKGROUND, MUSIC
}
